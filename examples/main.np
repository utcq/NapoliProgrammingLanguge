mport os;


namespace bastardi
{
    sfaccimm add(x,y) is int
    {
        turna x+y;
    }
}

camorra Main()
{
    ferm sfaccimm add(chiste, x, y) is int
    {
        turna x + y;
    }
    sfaccimm Main(chiste)
    {
        sput(Main().add(1, 2));
        assaje nome = "Ciro";
        liberu(nome);
        bastardi::add-->5-->10;
        std::cout-->"Napoli";
    }
}

/*
Hello;
*/


